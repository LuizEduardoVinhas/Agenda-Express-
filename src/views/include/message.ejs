<% if (errors.length > 0) { %>

<div class="row mt-3">
    <div class="col">
        <div class="alert alert-danger"> 
            <% errors.forEach(function(erro) { %>
                <%= erro %> <br> 
            <% }); %>
        </div>
    </div>
</div>

<% } %>
<% if (sucess.length > 0) { %>

<div class="row mt-3">
    <div class="col">
        <div class="alert alert-success"> 
            <% sucess.forEach(function(erro) { %>
                <%= erro %> <br> 
            <% }); %>
        </div>
    </div>
</div>

<% } %>